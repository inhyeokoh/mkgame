자바스크립트는 사용자와의 상호작용
Javascript 코드에 따라서 style 속성이 추가되면서 디자인을 바꿀 수 있는 것이다.
웹 브라우저에서 오른쪽 버튼 > 검사 를 누르면 뜨는 창을 잘 살펴보면 Console이라는 이름의 탭이 있는데, 이 콘솔을 이용하면 웹사이트에서 간단한 문제를 간편하게 Javascript를 실행시켜서 해결할 수 있다.
index : 색인 또는 목록이라는 의미이며, 데이터를 기록할 경우 그 데이터의 이름, 데이터 크기 등의 속성과 그 기록 장소 등을 표로 표시하는 것. 

.length 문자열의 갯수를 알려줌
.toUpperCase() 전부 대문자로 바꿔줌
.trim 공백을 지워줌

따옴표 안의 내용은 문자열